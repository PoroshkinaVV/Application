<%= form_for(@auditory) do |f| %>
  <% if @auditory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auditory.errors.count, "error") %> prohibited this auditory from being saved:</h2>

      <ul>
      <% @auditory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :building_id %><br>
    <%= f.text_field :building_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
